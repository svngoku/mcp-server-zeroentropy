# ZeroEntropy API Configuration
# Get your API key from: https://zeroentropy.com
ZEROENTROPY_API_KEY=your_api_key_here

# Optional: ZeroEntropy API Base URL (for EU cluster or custom endpoints)
# ZEROENTROPY_API_BASE_URL=https://eu.api.zeroentropy.com

# MCP Server Configuration
# Transport mode: stdio (default for Claude Desktop), http, or sse
ZEROENTROPY_MCP_TRANSPORT=stdio

# Port for HTTP/SSE transport modes (default: 8000)
ZEROENTROPY_MCP_PORT=8000

# Optional: Enable debug logging
# MCP_DEBUG=true